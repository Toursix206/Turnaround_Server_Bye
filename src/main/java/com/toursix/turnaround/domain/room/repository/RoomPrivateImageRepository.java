package com.toursix.turnaround.domain.room.repository;

import com.toursix.turnaround.domain.room.data.RoomPrivateImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoomPrivateImageRepository extends JpaRepository<RoomPrivateImage, Long> {
}
